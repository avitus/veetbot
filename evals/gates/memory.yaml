# Canonical gate registry entries for the memory area.
- id: gate.memory.contradiction
  milestone: 9
  kind: case
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**Contradiction handling** — inject a preference change; verify supersession (not duplication) and that retrieval returns the current belief."
  check: tests/gates/test_memory_m9.py::test_contradiction
- id: gate.memory.no_fabrication
  milestone: 9
  kind: corpus
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**No fabrication** — it must not form beliefs unsupported by episodes."
  check: tests/gates/test_memory_m9.py::test_no_fabrication
  corpus: evals/corpora/memory_no_fabrication/
  minimum_members: 3
- id: gate.memory.form_injection
  milestone: 9
  kind: corpus
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**Injection resistance** — an untrusted \"remember X\" must not form a belief."
  check: tests/gates/test_memory_m9.py::test_form_injection
  corpus: evals/corpora/memory_form_injection/
  minimum_members: 3
- id: gate.memory.correction_durable
  milestone: 9
  kind: case
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**Correction durability** — a rejected belief must not return, including after a full re-derivation under a newer consolidation policy. A hard gate."
  check: tests/gates/test_memory_m9.py::test_correction_durable
- id: gate.memory.no_policy_regress
  milestone: 9
  kind: case
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**No policy regression** — adapt LOCOMO-style long-horizon scenarios to exercise the write path. Gate: memory improves target eval cases **without** increasing policy failures."
  check: tests/gates/test_harness_m9.py::test_memory_changes_outcome
- id: gate.memory.multi_candidate
  milestone: 10
  kind: case
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**Multiple ordinary memories** — one ordinary user utterance naming two durable entities forms two separate, provenance-linked beliefs, and an independent preference in the same utterance remains a third belief."
  check: tests/gates/test_memory_m10.py::test_ordinary_conversation_forms_one_memory_per_durable_entity
- id: gate.memory.source_integrity
  milestone: 10
  kind: property
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**Automatic-source integrity** — automatic candidates name only source events authored by the owning principal; model, tool, and foreign-principal content cannot become a direct source."
  check: tests/gates/test_memory_m10.py::test_automatic_formation_uses_only_the_owning_principal_user_events
- id: gate.memory.idle_lifecycle
  milestone: 10
  kind: case
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**Idle lifecycle** — a terminal run enqueues one idempotent formation flag, returns without extracting, and maintenance consolidates the session only once both the session-idle boundary and the flag's persisted `not_before` have elapsed."
  check: tests/gates/test_memory_m10.py::test_terminal_run_is_flagged_then_consolidated_after_idle
- id: gate.memory.formation_bounded
  milestone: 10
  kind: case
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**Bounded automatic formation** — a pathological utterance cannot commit more than twelve candidates in one consolidation, a secret-shaped candidate is still rejected, and the consolidation audit accounts for extractor overproduction rather than silently truncating it."
  check: tests/gates/test_memory_m10.py::test_automatic_formation_caps_candidates_and_rejects_secrets
- id: gate.memory.correction_isolated
  milestone: 10
  kind: case
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**Ordinary correction isolation** — a natural-language retraction supersedes the matching entity while unrelated entities and preference topics continue to coexist."
  check: tests/gates/test_memory_m10.py::test_ordinary_retraction_supersedes_the_owned_entity_memory
- id: gate.memory.inspection_governed
  milestone: 10
  kind: case
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**Governed inspection and reversibility** — the public memory surface exposes beliefs, formation audits, recall traces, session diagnosis, and provenance-preserving replay through the same principal-scoped service used for edits and deletion; a foreign principal can observe or mutate none of them."
  check: tests/gates/test_memory_m10.py::test_memory_inspection_surface_is_governed_and_traceable
- id: gate.memory.extractor_contract
  milestone: 10
  kind: structural
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**Extractor contract coverage** — every shipped MemoryCandidateExtractor implementation is registered against the shared contract for separate, scope-correct, provenance-bound proposals."
  check: tests/contract/test_memory_candidate_extractor_contract.py::test_extractor_returns_separate_provenance_bound_candidate_proposals
- id: gate.memory.provider_activation_bound
  milestone: 10
  kind: property
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**Evidence-bound activation** — provider assistance is eligible only on an exact extractor, formation-policy, model-policy, provider, model, policy-profile, and compiled-policy tuple match."
  check: tests/gates/test_memory_m10.py::test_provider_activation_requires_an_exact_evaluation_tuple
- id: gate.memory.provider_boundary
  milestone: 10
  kind: case
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**Provider provenance boundary** — provider proposals remain proposals: invented source ids and secret-shaped source content cannot pass the governed service's provenance and safety checks."
  check: tests/unit/test_memory_provider_extraction.py::test_provider_proposals_still_pass_source_and_secret_gates
- id: gate.memory.provider_audit_fallback
  milestone: 10
  kind: case
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**Audited deterministic fallback** — a failed provider attempt records safe normalized failure fields and returns independently derived deterministic proposals."
  check: tests/unit/test_memory_provider_extraction.py::test_provider_failure_is_audited_and_uses_deterministic_fallback
- id: gate.memory.provider_evidence_publish
  milestone: 10
  kind: case
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**Evidence publication** — the checked-in paired evaluation derives the active provider tuple and corpus hash and atomically publishes activation evidence only after formation lift, at least eighty percent positive-case coverage, zero fabrication in either arm, and no policy regression pass; a failed evaluation leaves no artifact."
  check: tests/unit/test_memory_formation_eval.py::TestProviderEvidencePublicationGate::test_pass_publishes_the_exact_tuple
- id: gate.memory.provider_claim_rendering
  milestone: 10
  kind: structural
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**Canonical provider claims** — the provider returns a closed semantic claim with an exact cited evidence substring; deterministic local rendering owns the final belief type, canonical subject, statement, and authorized scope."
  check: tests/unit/test_memory_provider_extraction.py::test_provider_semantic_claims_render_canonical_memories
- id: gate.memory.provider_failure_diagnostics
  milestone: 10
  kind: case
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**Inspectable evaluation failure** — paired evaluation results retain each arm's normalized beliefs, consolidation counts, provider audit outcome and candidate counts, and shared-versus-provider-added attribution; a failed gate returns those diagnostics and the CLI exits non-zero without publishing an activation artifact."
  check: tests/unit/test_memory_formation_eval.py::TestProviderEvidencePublicationGate::test_failure_returns_diagnostics_and_leaves_no_activation_artifact
- id: gate.memory.provider_positive_coverage
  milestone: 10
  kind: case
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**Positive formation coverage** — aggregate lift alone is insufficient: provider assistance must fully support at least sixteen of the twenty labeled positive cases before evidence can be published."
  check: tests/gates/test_memory_m10.py::test_provider_evidence_preserves_m10_positive_coverage_floor
- id: gate.memory.provider_source_safety
  milestone: 10
  kind: case
  spec: docs/plan/memory-formation-and-consolidation.md#hard-gates
  statement: "**Authoritative source safety** — secret, injection, or transient markers in authoritative cited source text cannot be erased by provider normalization before the automatic eligibility check."
  check: tests/gates/test_memory_m10.py::test_automatic_formation_rechecks_source_hazards_after_extraction
- id: gate.memory.currency
  milestone: 9
  kind: case
  spec: docs/plan/memory-retrieval-and-ranking.md#hard-gates
  statement: "**Currency** — after a preference change, retrieval must return the new belief and never the superseded one."
  check: tests/gates/test_memory_m9.py::test_currency
- id: gate.memory.historical_correct
  milestone: 9
  kind: case
  spec: docs/plan/memory-retrieval-and-ranking.md#hard-gates
  statement: "**Historical correctness** — an `as_of` query returns what was believed then."
  check: tests/gates/test_memory_m9.py::test_historical_correct
- id: gate.memory.recall_injection
  milestone: 9
  kind: corpus
  spec: docs/plan/memory-retrieval-and-ranking.md#hard-gates
  statement: "**Injection resistance** — a poisoned belief renders `[BLOCKED]` and does not alter behavior."
  check: tests/gates/test_memory_m9.py::test_recall_injection
  corpus: evals/corpora/memory_recall_injection/
  minimum_members: 3
- id: gate.memory.scope_isolation
  milestone: 9
  kind: property
  spec: docs/plan/memory-retrieval-and-ranking.md#hard-gates
  statement: "**Scope isolation** — zero cross-tenant and cross-principal results. A hard gate, not a metric to improve."
  check: tests/gates/test_memory_m9.py::test_scope_isolation
- id: gate.memory.trace_faithful
  milestone: 9
  kind: case
  spec: docs/plan/memory-retrieval-and-ranking.md#hard-gates
  statement: "**Trace faithfulness** — for sampled turns, the beliefs listed in the trace must reproduce the rendered block that the recorded hash covers. A hard gate, not a metric to improve."
  check: tests/gates/test_memory_m9.py::test_trace_faithful
- id: gate.memory.view_ceiling
  milestone: 9
  kind: property
  spec: docs/plan/memory-retrieval-and-ranking.md#hard-gates
  statement: "**View ceiling** — no belief above `min(recall ceiling, viewing ceiling)` may ever appear in a view. A hard gate, not a metric to improve."
  check: tests/gates/test_memory_m9.py::test_view_ceiling
- id: gate.memory.retr_correction
  milestone: 9
  kind: case
  spec: docs/plan/memory-retrieval-and-ranking.md#hard-gates
  statement: "**Correction durability** — a rejected belief does not return, including across a consolidation-policy upgrade and a full re-derivation."
  check: tests/gates/test_memory_m9.py::test_retrieval_correction
- id: gate.memory.cache_preserved
  milestone: 9
  kind: case
  spec: docs/plan/memory-retrieval-and-ranking.md#hard-gates
  statement: "**Cache preservation** — measured cached-token ratio must not regress when memory is enabled. This is the invariant's regression test."
  check: tests/gates/test_memory_m9.py::test_cache_preserved
- id: gate.memory.no_triple_regress
  milestone: 9
  kind: case
  spec: docs/plan/memory-retrieval-and-ranking.md#hard-gates
  statement: "**No triple regression** — retrieval improves target eval cases **without** increasing policy failures, without regressing cache utilization, and without raising noise ratio."
  check: tests/gates/test_memory_m9.py::test_no_triple_regress
- id: gate.memory.provider_positive_coverage_v2
  milestone: 16
  kind: case
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**Expanded positive formation coverage is current.** Provider assistance must fully support at least seventeen of the twenty-one labeled positive cases in the expanded corpus before Milestone 16 evidence can be published."
  check: tests/unit/test_memory_formation_eval.py::TestProviderEvidencePublicationGate::test_lift_without_minimum_positive_coverage_does_not_publish
- id: gate.memory.bench_corpus_shape
  milestone: 16
  kind: structural
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**The corpus is shaped as declared.** Loading the checked-in corpus yields at least twelve scenarios, every scenario at least two sessions, at least three probes in every category, and at least four protected-abstention scenarios; every category rule, identifier uniqueness rule, and label reference resolves, and a corpus violating any of them is refused."
  check: tests/gates/test_memory_m16.py::test_bench_corpus_shape
- id: gate.memory.bench_reproducible
  milestone: 16
  kind: property
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**The deterministic arm is reproducible.** Two runs of the same commit over the same corpus produce identical aggregate metrics and identical per-probe rows, and no probe records more than one distinct cacheable prefix."
  check: tests/gates/test_memory_m16.py::test_bench_deterministic_arm_is_reproducible
- id: gate.memory.bench_no_regression
  milestone: 16
  kind: case
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**The benchmark does not regress.** Comparing a fresh deterministic run against the checked-in baseline reports no drift and no regression, where regression is any higher-is-better count falling, any lower-is-better count rising, or any probe losing a recalled label."
  check: tests/gates/test_memory_m16.py::test_bench_deterministic_metrics_do_not_regress_baseline
- id: gate.memory.bench_baseline_current
  milestone: 16
  kind: case
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**The baseline is current.** A fresh deterministic run equals the checked-in baseline exactly, so a behavior change cannot land without re-recording it in the same change."
  check: tests/gates/test_memory_m16.py::test_bench_baseline_matches_current_run_exactly
- id: gate.memory.bench_protected_never_rendered
  milestone: 16
  kind: case
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**Protected content never forms and never renders.** Across the corpus, no belief of any status contains a protected fragment, no recall trace renders one, and no abstention probe leaks one."
  check: tests/gates/test_memory_m16.py::test_bench_protected_content_never_forms_or_renders
- id: gate.memory.bench_supersession_current
  milestone: 16
  kind: case
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**A formed update never renders what it superseded.** For every update and correction probe whose superseding belief formed, the superseded statement appears in no trace, and the unformed case is counted separately rather than reported as a currency failure."
  check: tests/gates/test_memory_m16.py::test_bench_formed_updates_never_render_superseded
- id: gate.memory.bench_evidence_publish
  milestone: 16
  kind: case
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**Live evidence exists only when every pass condition held.** A passing live run publishes one artifact carrying the corpus digest, build reference, provider, model, policy profile, compiled policy version, and both memory policy versions, and its validator re-derives every threshold from the counts inside it; a failing run returns diagnostics and writes no artifact, and an existing artifact is never overwritten."
  check: tests/unit/test_memory_benchmark_eval.py::TestBenchmarkEvidencePublicationGate::test_pass_publishes_exact_tuple
- id: gate.memory.bench_cost_ceiling
  milestone: 16
  kind: case
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**The live arm stops before it exceeds its ceiling.** The pre-admission check refuses the run that would cross USD 4.00, records that it stopped for the ceiling, and publishes nothing; a model priced at zero aborts the arm instead of defeating the check."
  check: tests/unit/test_memory_benchmark_eval.py::TestBenchmarkEvidencePublicationGate::test_cost_ceiling_stops_before_exceeding
- id: gate.memory.bench_external_adapters
  milestone: 16
  kind: structural
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**The external adapters map their datasets without vendoring them.** Each of the three loaders round-trips a synthetic fixture shaped like its dataset into scenarios, labeled beliefs, and probes with evidence references, and the repository contains no dataset file."
  check: tests/unit/test_memory_benchmark_external.py::test_adapters_round_trip_synthetic_fixtures
- id: gate.memory.profiles_wired
  milestone: 16
  kind: structural
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**Memory profiles are loaded and mirror the shipped defaults.** The shipped profile document validates to exactly the default models, an operator overlay changes the value the retriever uses, unknown keys are rejected, and the session idle boundary is not in the document."
  check: tests/gates/test_memory_m16.py::test_memory_profiles_are_loaded_and_mirror_shipped_defaults
- id: gate.memory.trace_retention
  milestone: 16
  kind: case
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**Operator trace fields expire on schedule.** A trace past its operator expiry loses its arm latencies, candidate counts, and dropped-for-budget identifiers while its user-safe fields survive intact, an unexpired trace is untouched, and a second sweep changes nothing."
  check: tests/gates/test_memory_m16.py::test_trace_operator_fields_expire_on_schedule
- id: gate.memory.decay_lifecycle
  milestone: 16
  kind: case
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**Unused provisional beliefs decay and retire.** Under the sweep an idle provisional belief loses confidence and one below the floor is retired with its validity closed, while an active user-stated belief and a recently reinforced belief are untouched."
  check: tests/gates/test_memory_m16.py::test_decay_sweep_lowers_unused_provisional_and_retires_below_floor
- id: gate.memory.usage_feedback
  milestone: 16
  kind: case
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**Citing a belief records usage without manufacturing evidence.** A belief whose short identifier appears in a completed run's final message is marked cited on its trace, gains utility and a fresh usage instant while its evidence and confidence stay fixed; a returned-but-uncited belief loses utility; repeating the completion changes nothing."
  check: tests/gates/test_memory_m16.py::test_usage_feedback_marks_cited_and_never_raises_confidence
- id: gate.memory.recall_delta
  milestone: 16
  kind: case
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**The recall delta surfaces beliefs formed after the snapshot.** A belief written after a session's snapshot is injected in the next turn through the delta query bounded by the snapshot watermark, without rewriting the cached prefix."
  check: tests/gates/test_memory_m16.py::test_recall_delta_surfaces_post_snapshot_beliefs
- id: gate.memory.correction_lines
  milestone: 16
  kind: case
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**Correction lines are never yielded and never move the prefix.** When a snapshot member is superseded mid-session the next turn carries its correction line, that line survives budget pressure that drops recall blocks, it is never offered for yielding, and the prefix digest is unchanged."
  check: tests/gates/test_memory_m16.py::test_snapshot_correction_lines_never_yield_and_prefix_is_stable
- id: gate.memory.established_facts_form
  milestone: 16
  kind: case
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**Established facts enter formation as affirmed candidates.** A fact established in working state with non-empty provenance whose every source is a trusted user event of the owning principal becomes a committed belief at affirmed authority carrying that provenance, while empty provenance, an untrusted source, or a foreign source forms nothing."
  check: tests/gates/test_memory_m16.py::test_established_facts_enter_formation_as_affirmed_candidates
- id: gate.memory.conflict_surfaced
  milestone: 16
  kind: case
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**A lower-authority contradiction is conflicted, not resolved.** An inference contradicting a user-stated belief commits flagged and linked in both directions, requests confirmation, leaves the original live, and both statements surface together in recall with the link rendered."
  check: tests/gates/test_memory_m16.py::test_lower_authority_contradiction_is_conflicted_and_both_surface
- id: gate.memory.authority_recency
  milestone: 16
  kind: property
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**The resolver orders authority then recency.** Over generated authority pairs and source orderings, higher authority never loses to lower, equal authority with a later source or instant supersedes, equal authority with no ordering conflicts, and polarity alone never conflicts."
  check: tests/gates/test_memory_m16.py::test_resolver_orders_authority_then_recency
- id: gate.memory.rederive_opt_in
  milestone: 16
  kind: case
  spec: docs/plan/memory-evaluation-and-lifecycle.md#hard-gates
  statement: "**Re-derivation is opt-in and replays rejections.** The command refuses without an explicit confirmation, and with it re-consolidates from watermark zero without resurrecting a rejected belief."
  check: tests/gates/test_memory_m16.py::test_replay_is_opt_in_and_replays_rejections
- id: gate.memory.read_api_ceiling_required
  milestone: 17
  kind: case
  spec: docs/plan/memory-read-api-and-browser.md#hard-gates
  statement: "**A request without a ceiling is refused.** A list or detail request that omits `ceiling` is a validation error naming the parameter; the server returns no belief and applies no default. A ceiling outside the enumeration is the same error."
  check: tests/gates/test_memory_read_api_m17.py::test_a_request_without_a_ceiling_is_refused
- id: gate.memory.read_api_ceiling_filter
  milestone: 17
  kind: property
  spec: docs/plan/memory-read-api-and-browser.md#hard-gates
  statement: "**Nothing above the ceiling is returned or distinguishable.** Over generated belief sensitivities and request ceilings, no item of any list page has a sensitivity above the requested ceiling, and a detail read of a belief above it is byte-identical to a detail read of an identifier that does not exist."
  check: tests/gates/test_memory_read_api_m17.py::test_nothing_above_the_ceiling_is_returned_or_distinguishable
- id: gate.memory.read_api_principal_isolation
  milestone: 17
  kind: case
  spec: docs/plan/memory-read-api-and-browser.md#hard-gates
  statement: "**A principal sees only its own beliefs.** A list request returns nothing belonging to another principal or another tenant, and a detail request for such a belief is `not_found` with status 404 rather than `authorization_error`."
  check: tests/gates/test_memory_read_api_m17.py::test_a_principal_sees_only_its_own_beliefs
- id: gate.memory.read_api_pagination
  milestone: 17
  kind: case
  spec: docs/plan/memory-read-api-and-browser.md#hard-gates
  statement: "**Keyset paging neither skips nor repeats.** Walking every page while beliefs are written and retired between requests yields each live belief at most once and every belief that existed throughout exactly once; a malformed cursor is a validation error; `next_cursor` is null on the last page and only there; and re-reading a cursor against an unchanged store returns an identical page."
  check: tests/gates/test_memory_read_api_m17.py::test_keyset_paging_neither_skips_nor_repeats
- id: gate.memory.read_api_filters
  milestone: 17
  kind: case
  spec: docs/plan/memory-read-api-and-browser.md#hard-gates
  statement: "**Every filter selects the documented set.** Status, belief type, subject, source session, and text each select exactly the set the design declares and compose with one another; the default status set is the live one; and the text filter's results equal those of the shared lexical helpers applied directly."
  check: tests/gates/test_memory_read_api_m17.py::test_every_filter_selects_the_documented_set
- id: gate.memory.read_api_read_only
  milestone: 17
  kind: structural
  spec: docs/plan/memory-read-api-and-browser.md#hard-gates
  statement: "**The router is read-only.** Every route mounted under `/v1/memories` declares the GET method and exactly the scope `memory.read`; a route with any other method or any other scope fails the build."
  check: tests/gates/test_memory_read_api_m17.py::test_the_router_is_read_only
- id: gate.memory.read_api_flag_absent
  milestone: 17
  kind: case
  spec: docs/plan/memory-read-api-and-browser.md#hard-gates
  statement: "**The flag is a real switch.** With `AGENT_MEMORY_API_ENABLED` unset, no `/v1/memories` route is registered and none appears in the OpenAPI document, while `memory.read` remains a recognized scope for configuration validation."
  check: tests/gates/test_memory_read_api_m17.py::test_the_flag_is_a_real_switch
- id: gate.memory.browse_contract_parity
  milestone: 17
  kind: structural
  spec: docs/plan/memory-read-api-and-browser.md#hard-gates
  statement: "**Both stores browse identically.** The shared browse contract suite covers order, the keyset boundary, every filter, and the text query against the in-memory adapter, and the PostgreSQL parity suite answers the same `MemoryBrowseQuery` values from a live store and compares the two adapters over one corpus; both adapters derive their query terms through the same shared helper and lowercase their subject comparison alike."
  check: tests/gates/test_memory_read_api_m17.py::test_both_stores_browse_identically
- id: gate.memory.read_api_view_projection
  milestone: 17
  kind: structural
  spec: docs/plan/memory-read-api-and-browser.md#hard-gates
  statement: "**The projection is exactly the exposure list.** A serialized `MemoryView` carries every field the exposure list names and no other key, and no construction path — list, detail, or error envelope — can emit a withheld field."
  check: tests/gates/test_memory_read_api_m17.py::test_the_projection_is_exactly_the_exposure_list
- id: gate.memory.read_api_error_vocabulary
  milestone: 17
  kind: case
  spec: docs/plan/memory-read-api-and-browser.md#hard-gates
  statement: "**Every error is a member of the closed vocabulary.** Across missing and malformed parameters, absent and insufficient credentials, unknown identifiers, and above-ceiling reads, every response either succeeds or carries one of `malformed_request`, `authentication_error`, `authorization_error`, or `not_found` with its documented status."
  check: tests/gates/test_memory_read_api_m17.py::test_every_error_is_a_member_of_the_closed_vocabulary
- id: gate.memory.distill_versions_frozen
  milestone: 21
  kind: structural
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Completed policy controls are frozen.** The recorded formation@7 and formation@8 fixtures retain byte-identical normalized outputs while formation@9 identifies nemori-assisted-v1."
  check: tests/gates/test_memory_m21.py::test_completed_policy_controls_are_frozen
- id: gate.memory.episode_integration
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Integrated episodes are coherent and grounded.** Multi-turn narratives preserve ordered owned provenance and unsupported or foreign citations are rejected."
  check: tests/gates/test_memory_m21.py::test_integrated_episodes_are_coherent_and_grounded
- id: gate.memory.episode_repository_parity
  milestone: 21
  kind: structural
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Both episode stores obey one contract.** Idempotency, scoped reads, deletion, and erasure behave identically in memory and PostgreSQL."
  check: tests/contract/test_integrated_episode_store_contract.py::test_integrated_episode_repository_contract_is_registered
- id: gate.memory.anticipation_blinded
  milestone: 21
  kind: property
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Anticipation is causally blinded.** The request contains only the causal cue and prior memories, never current or future evidence, assistant answers, or gold labels."
  check: tests/gates/test_memory_m21.py::test_anticipation_is_causally_blinded
- id: gate.memory.prediction_error_calls
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**One consolidation makes exactly three batched provider calls.** Candidate and event count never adds a provider call."
  check: tests/gates/test_memory_m21.py::test_one_consolidation_makes_exactly_three_batched_calls
- id: gate.memory.distill_fallback
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Every provider stage has an audited deterministic fallback.** Failures record normalized metadata and never partly trust invalid output."
  check: tests/gates/test_memory_m21.py::test_every_provider_stage_has_an_audited_deterministic_fallback
- id: gate.memory.direct_high_recall
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Direct ongoing projects form at high recall.** The personal-agent statement forms a direct ongoing-project memory with exact user provenance."
  check: tests/gates/test_memory_m21.py::test_personal_agent_project_forms_as_direct_ongoing_memory
- id: gate.memory.hypothesis_high_recall
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Useful hypotheses form as hypotheses.** The personal-agent cue may form likely software-development experience only as tentative, never as a direct occupation."
  check: tests/gates/test_memory_m21.py::test_software_development_cue_forms_only_as_tentative_hypothesis
- id: gate.memory.compound_recall
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Compound formation is bounded and diverse.** Directness, usefulness, and subject diversity govern the six-per-source and thirty-two-global ceilings with every displacement counted."
  check: tests/gates/test_memory_m21.py::test_compound_formation_is_bounded_diverse_and_accounted
- id: gate.memory.candidate_schema
  milestone: 21
  kind: structural
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**The candidate language is closed and complete.** Claim kind, derivation, longevity, and exact evidence spans validate as a closed schema."
  check: tests/gates/test_memory_m21.py::test_candidate_language_is_closed_and_complete
- id: gate.memory.predictability_attributed
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Predictability suppresses only attributable redundancy.** General model expectation cannot suppress a direct observation."
  check: tests/gates/test_memory_m21.py::test_predictability_suppresses_only_attributable_redundancy
- id: gate.memory.source_grounding
  milestone: 21
  kind: property
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Every automatic claim is source-grounded.** Exact spans resolve only to owned trusted user events."
  check: tests/gates/test_memory_m21.py::test_every_automatic_claim_is_source_grounded
- id: gate.memory.formation_reason_telemetry
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Decision telemetry accounts for every proposal.** Terminal reason counts reconcile without content in telemetry."
  check: tests/gates/test_memory_m21.py::test_decision_telemetry_accounts_for_every_proposal
- id: gate.memory.evidence_clock
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Only later evidence refreshes the evidence clock.** Supporting user events advance evidence time and counters exactly once; replay and citation do not."
  check: tests/gates/test_memory_m21.py::test_only_later_evidence_refreshes_the_evidence_clock
- id: gate.memory.usage_clock
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Usage has its own clock.** Citation changes last-used time and utility only and completion replay is idempotent."
  check: tests/gates/test_memory_m21.py::test_usage_has_its_own_clock
- id: gate.memory.hypothesis_retirement
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Unsupported hypotheses retire after thirty days.** Retrieval and citation cannot extend the boundary."
  check: tests/gates/test_memory_m21.py::test_unsupported_hypotheses_retire_after_thirty_days
- id: gate.memory.ongoing_retirement
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Unsupported ongoing observations retire after ninety days.** Self-use cannot extend the boundary."
  check: tests/gates/test_memory_m21.py::test_unsupported_ongoing_observations_retire_after_ninety_days
- id: gate.memory.evidence_promotion
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Later evidence promotes rather than duplicates.** Direct support promotes the existing hypothesis and preserves combined provenance."
  check: tests/gates/test_memory_m21.py::test_later_evidence_promotes_instead_of_duplicating
- id: gate.memory.uncertainty_rendered
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Recall renders uncertainty faithfully.** Derivation and longevity appear on governed context, trace, CLI, and API views without raw model scores."
  check: tests/gates/test_memory_m21.py::test_recall_renders_uncertainty_faithfully
- id: gate.memory.correction_durable_v3
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Corrections remain durable through the new pipeline.** Integration, retry, replay, and policy upgrades cannot recreate a corrected memory."
  check: tests/gates/test_memory_m21.py::test_corrections_remain_durable_through_distillation
- id: gate.memory.schema_backfill
  milestone: 21
  kind: structural
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**The schema migration and backfill preserve history.** Conservative derived values retain old statement, provenance, authority, and formation version and erasure removes new rows."
  check: tests/integration/test_memory_distillation_postgres_m21.py::test_schema_backfill_preserves_history_and_erasure
- id: gate.memory.formation_corpus_v3
  milestone: 21
  kind: structural
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Corpus v3 has the declared coverage.** At least sixty cases, seventy percent positive, every category, and all six core scenarios validate."
  check: tests/gates/test_memory_m21.py::test_formation_corpus_v3_has_declared_coverage
- id: gate.memory.comparative_evidence
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Comparative evidence proves marked useful-recall lift.** One run compares formation@7, formation@8, and formation@9 and publishes only at every declared threshold."
  check: tests/gates/test_memory_m21.py::test_comparative_evidence_proves_marked_useful_recall_lift
- id: gate.memory.distill_activation_bound
  milestone: 21
  kind: property
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Activation is exact and evidence-bound.** Formation@9 activates only for the exact artifact tuple; auto keeps the older evidenced policy and required refuses otherwise."
  check: tests/gates/test_memory_m21.py::test_distillation_activation_is_exact_and_evidence_bound
- id: gate.memory.scorer_symmetric
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**The comparative scorer cannot be fooled by supersets, negations, counts, or siblings.** A belief that elaborates beyond, negates, recounts, or names a sibling activity of a gold claim never scores as a match, and a generic user subject never matches a specific conflict key."
  check: tests/gates/test_memory_m21.py::test_comparative_scorer_rejects_supersets_negations_counts_and_siblings
- id: gate.memory.formation_corpus_seeded
  milestone: 21
  kind: structural
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Comparative evidence runs against a populated store.** The corpus declares a seed pool of at least twenty-five prior beliefs, the rich production conversation and another multi-event case run against it, and the evaluator writes the seeds through the governed service before consolidating."
  check: tests/gates/test_memory_m21.py::test_formation_corpus_v3_runs_against_a_populated_store
- id: gate.memory.fallback_never_fabricates
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**The deterministic fallback never fabricates.** Turns that share surface phrasing with the production training conversation but state no durable training fact form nothing except the stated wish for age-aware recommendations, and a long or unterminated turn neither crashes nor stalls consolidation."
  check: tests/gates/test_memory_m21.py::test_high_recall_fallback_never_fabricates_from_control_turns
- id: gate.memory.coverage_dispositions_verified
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**A represented clause is verified against the memory it cites.** A coverage unit marked represented counts only when the attributed live memory asserts the same claim and is about that clause; otherwise it is recorded as unverified and the stage completes partially rather than trusting the label."
  check: tests/gates/test_memory_m21.py::test_represented_coverage_is_verified_against_the_cited_memory
- id: gate.memory.distill_segmentation
  milestone: 21
  kind: case
  spec: docs/plan/adaptive-memory-distillation.md#hard-gates
  statement: "**Long batches segment into bounded three-call rounds.** A batch beyond one ledger's clause bound runs exactly three calls per segment, persists one episode set per segment, and records per-segment stage metrics; a segment's ledger and candidates fit the output ceiling."
  check: tests/gates/test_memory_m21.py::test_long_batches_are_segmented_into_bounded_three_call_rounds
